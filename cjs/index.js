const clone = require('./clone');
const del = require('./del');
const get = require('./get');
const pick = require('./pick');
const set = require('./set');

module.exports = { clone, del, get, pick, set };
